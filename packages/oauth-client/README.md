# @dsh-plugin/oauth-client

Host half is an empty `apply`. The browser half (`./client` → `browser.js`) is
a `window.__ModuleLoader__` bundle that registers Settings → Grok / xAI with a
Login button. The Host also still serves `/dsh-oauth/xai/`.
